- [ ] Update `supabase_schema.sql` to add:
  - [ ] deck modeling for default(rule-based) + custom(explicit deck_cards)
  - [ ] `user_sync_state` cursor per logged-in user
  - [ ] proper indexes for sync (range by `updated_at`, upserts by `(user_id, kanji_id)` etc.)
  - [ ] consistent RLS policies
- [ ] Update `lib/services/supabase_service.dart` to remove placeholder keys and ensure Supabase initializes correctly.
- [ ] Update `lib/services/sync_service.dart`:
  - [ ] stop using `SharedPreferences` sync cursor
  - [ ] use Supabase `user_sync_state`
  - [ ] sync kanji_progress and deck definitions/custom deck membership appropriately
- [ ] Open review/practice flow files, then wire “auto-sync” on local changes (SRS update, deck edit, deck card changes).
